# 新建Eclipse 動態Web專案時覺得可以先略過的部分
.settings
/build/
.classpath
/log/
*.log
/target/

## 先移除webapp目錄, 避免初期xml跟測試資料互相蓋來蓋去
/src/main/webapp/

# 通常會包含依賴的配置, 每個人的環境可能會不太一樣, 先略過看看
.project

### 以下為網路上抄來建議忽略的檔案
# Eclipse
.metadata
bin/
tmp/
*.tmp
*.bak
*.swp
*~.nib
local.properties
.settings/
.loadpath
.recommenders

# External tool builders
.externalToolBuilders/

# Locally stored "Eclipse launch configurations"
*.launch

# STS (Spring Tool Suite)
.springBeans

# Code Recommenders
.recommenders/

# Annotation Processing
.apt_generated/
.apt_generated_test/

### Eclipse Patch ###
# Spring Boot Tooling
.sts4-cache/